### Added
- **FCR merge handoffs can carry the Cloudflare Worker version that emitted them.** An opt-in external Wrangler deployment helper enables Version Metadata and tags the Worker version with a Git-shaped source-commit claim without changing merge behavior or granting commit authority.
